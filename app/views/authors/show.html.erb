<h1><%= @author.name %></h1>

<h2>Books</h2>
<ul>
  <% @books.each do |book| %>
    <li><%= link_to book.title, book_path(book) %></li>
  <% end %>
</ul>

<%= link_to '⬅️ Go Back', request.referer || authors_path, class: 'btn btn-secondary mt-4' %>

