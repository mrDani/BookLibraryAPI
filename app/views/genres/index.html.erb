<h1 class="text-center">📚 Genres</h1>

<ul>
  <% @genres.each do |genre| %>
    <li><%= link_to genre.name, genre_path(genre) %></li>
  <% end %>
</ul>

<%= link_to '⬅️ Back to Books', books_path, class: 'btn btn-secondary mt-4' %>

